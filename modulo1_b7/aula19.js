// condicional ternário
// if em uma linha só
// operador = ?
// modelo -> ? true : false (valor que deu certo : valor quando dá "errado")

let isMember = true;

let shipping = isMember ? 2 : 10;

console.log(isMember ? "Você é membro" : "Você não é membro");

console.log("frete: " + shipping);
